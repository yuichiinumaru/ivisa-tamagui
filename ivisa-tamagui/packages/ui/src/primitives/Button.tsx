import { Button as TamaguiButton, styled } from 'tamagui';

// For now, we just re-export the Tamagui button.
// We can add custom variants here later.
export const IvisaButton = styled(TamaguiButton, {
  name: 'IvisaButton',
});
